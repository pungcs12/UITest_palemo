pipeline {
    agent any
    stages {
        stage('git repo'){
            steps{
                cleanWs()
                bat "git clone https://github.com/pungcs12/UITest_palemo.git"
            }
        }
        stage ('Install Dependencies') {
            steps {
                echo 'testing'
                pip list
                // script{
                //     sh '''
                //         ls
                //     '''
                // }
            }
        }
    }
}